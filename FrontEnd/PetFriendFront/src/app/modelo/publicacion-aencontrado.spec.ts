import { PublicacionAEncontrado } from './publicacion-aencontrado';

describe('PublicacionAEncontrado', () => {
  it('should create an instance', () => {
    expect(new PublicacionAEncontrado()).toBeTruthy();
  });
});
