import { PublicacionAPerdido } from './publicacion-aperdido';

describe('PublicacionAPerdido', () => {
  it('should create an instance', () => {
    expect(new PublicacionAPerdido()).toBeTruthy();
  });
});
