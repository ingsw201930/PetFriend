import { Animal } from './animal';

export class PublicacionAPerdido {
    descripcion: string;
    localidad: string;
    animalAsociado: Animal;
    encontrado: boolean;
    lugarPerdido: String;
}
