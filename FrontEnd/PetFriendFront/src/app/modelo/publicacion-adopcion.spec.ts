import { PublicacionAdopcion } from './publicacion-adopcion';

describe('PublicacionAdopcion', () => {
  it('should create an instance', () => {
    expect(new PublicacionAdopcion()).toBeTruthy();
  });
});
